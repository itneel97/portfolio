import React from 'react';
import classes from './Resume.module.css';

const resume = () => {
    return(
        <div className={classes.Main} >
            <h1 className={classes.temp}>I Know I should work on this first ;)</h1>
        </div>        
    );
}

export default resume;